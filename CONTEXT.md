# Pretty Properties QOL

本文件记录项目中与用户需求相关的核心术语，帮助需求、代码和后续讨论保持一致。

## 视觉属性

**视觉角色**：本项目要设置的三种用途：`icon`、`cover`、`banner`。它描述图片在笔记中的用途，而不是某个具体 UI 元素。
_避免_：把视觉角色直接称为视觉元素。

**属性映射**：视觉角色到实际 Obsidian property 名称的对应关系。默认映射是 `icon`、`cover`、`banner`，用户可以修改。
_避免_：固定属性名。

**源图片**：用户从当前笔记正文中选中的、指向仓库内图片文件的嵌入图片。
_避免_：目标图片、外部图片（除非明确指文件选择器选中的文件）。

**属性行按钮**：显示在 Obsidian 原生 property 行右侧操作区、用于为该属性选择或导入图片的按钮。
_避免_：Pretty Properties 图片按钮、视觉元素按钮。

## 文件与属性值

**导入目录**：用户从外部文件选择器选中的图片被复制到的仓库内相对目录，默认是仓库根目录，可配置。
_避免_：附件目录（除非用户将导入目录配置为附件目录）。

**图片属性值**：写入 YAML properties 的单个 wikilink。可配置为最短无歧义链接（默认，例如 `[[image.png]]`）或完整仓库路径（例如 `[[attachments/image.png]]`）。
_避免_：把 `![[...]]` 作为本项目的规范写入格式。

**图片属性命名策略**：导入外部图片时决定仓库内文件名的用户设置，可选原始文件名、笔记名加视觉角色名、手动命名。
_避免_：把属性值名称和图片文件名混为一谈。

**图片属性准备命令**：后续 TODO 中用于补充当前笔记缺失的图片相关 properties、并将它们相邻排列的命令。
_避免_：覆盖已有图片属性的重建命令。

**附加视觉属性**：Pretty Properties 中与图片展示方式相关、但不是图片文件本身的属性，例如 `cover_shape`、`cover_position`。它们属于后续扩展，不属于当前第一阶段的图片设置契约。
_避免_：把附加视觉属性当作源图片或图片属性值。
